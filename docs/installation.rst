.. highlight:: shell

============
Installation
============

At the command line::

    $ pip install dicompyler-core

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dicompylercore
    $ pip install dicompyler-core
