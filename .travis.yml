language: python
python:
    - 2.7
    - 3.3
    - 3.4
    - 3.5
install:
    - pip install numpy
    - pip install coverage
    - pip install coveralls
script:
  coverage run --source=dicompylercore setup.py test
after_success:
  coveralls
